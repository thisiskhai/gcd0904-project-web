{% extends 'base.html.twig' %}

{% block body %}
    <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Id</th>
                <th>name</th>
            </tr>
            </thead>
            <tbody>
            {% for cate in category %}
                <tr>
                    <th scope="row">{{ cate.id }}</th>
                    <td>{{ cate.name }}$</td>
                    <td>
                        {#                        <a href="{{ path('todo_details', {'id': todo.id}) }} " class='btn btn-success'>View</a>#}
                        {#                        <a href="{{ path('todo_edit', {'id': todo.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#                        <a href="{{ path('todo_delete', {'id': todo.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{ path('products_details', {'id': cate.id}) }} " class='btn btn-success'>View</a>
                        <a href="{{ path('products_edit', {'id': cate.id}) }}" class='btn btn-default'>Edit</a>
                        <a href="{{ path('products_delete', {'id': cate.id}) }}" class='btn btn-danger'>Delete</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

